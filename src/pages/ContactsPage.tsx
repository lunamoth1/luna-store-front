import React from "react";
import "../styles/contactsPage.css";

const ContactsPage: React.FC = () => {
	return (
		<div className="contacts-container">
			<h1 className="contacts-title">Contacts page</h1>
		</div>
	);
};

export default ContactsPage;
