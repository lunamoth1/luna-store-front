{
	"version": 2,
	"framework": "vite",
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/static-build",
			"config": {
				"distDir": "dist"
			}
		}
	],
	"routes": [
		{
			"src": "/(assets|src)/(.*)",
			"dest": "/$1/$2"
		},
		{
			"src": "/(.*)",
			"dest": "/index.html"
		}
	]
}
